# VoiceMood — laptop dependencies only.
# The Colab step has its own (heavier) requirements in the notebook header.
# These all install cleanly on Windows / macOS / Linux with Python 3.10 or 3.11.

numpy>=1.24,<2.0
scipy>=1.10
pandas>=2.0
scikit-learn>=1.3
matplotlib>=3.7
seaborn>=0.12

# Audio (laptop-side: only used for MFCC + waveform display, not deep models)
librosa>=0.10
soundfile>=0.12

# UI
streamlit>=1.30
plotly>=5.18

# ONNX export
onnx>=1.15
skl2onnx>=1.16
onnxruntime>=1.16

# Misc
joblib>=1.3
tqdm>=4.66
requests>=2.31
